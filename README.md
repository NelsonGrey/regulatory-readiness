# Regulatory Readiness Engine

**Status:** Proposed / discovery
**Baseline date:** August 30, 2026
**Working product name:** Regulatory Readiness Engine (placeholder; not trademark-cleared)

A multi-tenant platform that helps small and mid-size organizations assemble, validate, version, and export the evidence needed to prepare for a specific regulation — keeping missing, conflicting, stale, and unreviewed information visible, and never claiming legal compliance, certification, or authority approval.

The platform is **one reusable engine** plus a library of **control packs**. Each pack encodes one regulation as a dated, versioned catalog of discrete controls, with applicability rules, evidence expectations, validators, and export profiles. Adding a regulation means adding a pack — mostly data and declared rules, not a code fork.

## Start here

| Document | Purpose |
| --- | --- |
| [docs/ENGINE_CONCEPT.md](docs/ENGINE_CONCEPT.md) | Platform framing: engine primitives, the control-pack contract, the pack portfolio and sequencing, the fit test, next steps |
| [docs/engine/README.md](docs/engine/README.md) | Vertical-neutral requirements and design — start of the engine spec |
| [docs/engine/BUSINESS_REQUIREMENTS.md](docs/engine/BUSINESS_REQUIREMENTS.md) | Customers, personas, jobs, `BR-*`, readiness policy, commercial model, MVP acceptance |
| [docs/engine/TECHNICAL_REQUIREMENTS.md](docs/engine/TECHNICAL_REQUIREMENTS.md) | Architecture, domain model, control/version model, `TR-*`, extraction, APIs, nonfunctional targets, `FSG-*` |
| [docs/engine/detailed-design/README.md](docs/engine/detailed-design/README.md) | Personas, IA, screens, states, responsive/accessibility contracts, traceability, `AC-*`, build sequence |
| [docs/ARCHITECTURE_AWS.md](docs/ARCHITECTURE_AWS.md) | AWS reference architecture: region and account topology, service mapping per engine primitive, ingestion/malware pipeline, nonfunctional mapping, cost anchors, open AWS decisions |
| [docs/adr/](docs/adr/) | Decision records: 0001 AWS, 0002 tenancy (pooled + RLS), 0003 authorization, 0004 audit + outbox, 0005 control-pack packaging |
| [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) | Local-first dev setup, workspace layout, AWS free-tier reality and cost-control simplifications |
| [docs/packs/README.md](docs/packs/README.md) | Control-pack registry and the per-pack artifact contract |
| [docs/packs/espr-dpp-battery/](docs/packs/espr-dpp-battery/) | Origin design, preserved as the first control pack (EU battery passport) |
| [docs/marketing/README.md](docs/marketing/README.md) | Vision & build-handoff spec for the public marketing site — IA, messaging, language guardrails, tech shape, milestones |
| [docs/BUILD_STATUS.md](docs/BUILD_STATUS.md) | Retrospective after slice 28 — what shipped, the deliberate stand-ins and their swap-in points, and what a real launch still needs |

## Current state

Full specification, a buildable monorepo, and the first slices of real engine code. `typecheck` / `lint` / `lint:copy` / `test` (330) / `build` / `format` all green.

- **Spec** — engine spec split from the origin battery package into [`docs/engine/`](docs/engine/) + [`docs/packs/espr-dpp-battery/`](docs/packs/espr-dpp-battery/) (see [ENGINE_CONCEPT.md §9](docs/ENGINE_CONCEPT.md)); pack #1 spec in [`docs/packs/eaa-accessibility/`](docs/packs/eaa-accessibility/); ADRs 0001–0005 in [`docs/adr/`](docs/adr/).
- **Scaffold** — pnpm/TypeScript monorepo (`apps/{api,worker,web,marketing}`, `packages/*`, `packs/`, `infra/`) with a local Docker dev stack (Postgres + LocalStack).
- **Marketing site** — `@rre/marketing` (Astro): public pages (home, how-it-works, platform, packs + EAA landing, pricing, security, accessibility statement, about, blog, contact, legal/*), content collections, sitemap/rss/robots. `@rre/brand` holds the design tokens the app and site share; `@rre/copy-guard` holds the forbidden-phrase list, run over rendered copy by `pnpm lint:copy` in CI. See [docs/marketing/](docs/marketing/).
- **Slice 1 so far**
  - `@rre/contracts` — pack-artifact + API request schemas (Zod).
  - `packs/eaa-accessibility/` — a `draft` bundle (20 controls, 16 applicability rules, 5 known-outcome vectors) that loads + validates.
  - `packs/eu-battery-passport/` — a second `draft` bundle (EU Batteries Regulation, 8 controls, 7 rules, 4 vectors) proving the engine runs an unrelated regulation with no code change.
  - `@rre/control-catalog` — `loadPack` / `validatePack` / `loadInstalledPacks`, `validateEntityFacts`, and the deterministic applicability evaluator.
  - `@rre/domain` — applicability summary, canonical-JSON, entity types, and the deterministic **readiness derivation** (`deriveControlReadiness` per control → `readinessForEntity` → `EVIDENCE_READY` / `REVIEW_NEEDED` / `BLOCKED`); browser-safe.
  - `@rre/db` — shared Postgres pool, `withTenant` (RLS `SET LOCAL`), forward-only migration runner.
  - `@rre/api` — `GET /packs` + `GET /packs/:packKey`; `POST /entities` (immutable hashed scope evaluation); `GET /entities/:id/matrix` — now with **readiness per control** (`EVIDENCED`/`MISSING`/`PENDING_REVIEW`/`CONFLICTING`/…), the approved claim value, and the entity's overall status; **`POST /entities/:id/controls/:controlKey/claims`** (assert a claim → `PENDING_REVIEW`), **`POST /claims/:claimId/decisions`** (`APPROVED`/`REJECTED`/`CLARIFICATION_REQUESTED` — a reason is required to reject; approval supersedes the prior approved claim, history preserved), **`GET /entities/:id/review-queue`**, **`GET /audit-events`** (AUD-001). Tenant via `x-tenant-id`; cross-tenant reads empty/404.
  - **Persistence** — migrations `0001` (entities + scope evaluations, RLS `FORCE`d), `0002` (`rre_app` runtime role), `0003` (append-only `audit_event` + transactional `outbox`), `0004` (`rre_relay` BYPASSRLS role), `0005` (`claim` — value immutable, only `status`/`supersedes` writable by `rre_app` — + append-only `review_decision`), `0006` (`evidence_request` + `request_item` + `access_token_grant` (no RLS — resolved by token hash) + append-only `contributor_submission` / `contributor_response_item`), `0007` (`request_draft` — one mutable row per request, overwritten on save, deleted on submit), `0008` (`expire_lapsed_requests` — a `SECURITY DEFINER` function that flips lapsed requests to `EXPIRED` cross-tenant and writes their audit + outbox trail), `0009` (append-only `readiness_snapshot` — the frozen canonical export document + its content hash), `0010` (`notification` — per-tenant feed; only `read_at` is mutable to `rre_app`), `0011` (`document` — immutable key/hash/size, only lifecycle columns mutable, never hard-deleted — + append-only `document_association`), `0012` (append-only `evidence_location` + `claim_evidence_link`), `0013` (`extraction_run` + `extraction_proposal` — append-oriented; only the outcome / decision columns move), `0014` (`applicability_override` — append-only except `revoked_at`), `0015` (`deletion_request` — the append-only-except-completion tombstone — + `purge_tenant()`, a `SECURITY DEFINER` function that deletes every tenant-owned row across every table, including the append-only ones `rre_app` cannot touch), `0016` (`app_user` + `tenant` + `membership` + `tenant_invite` — the SaaS tenancy control plane; no RLS, like `access_token_grant`, since they are read before a tenant context exists), `0017` (`subscription` — one per workspace; control plane, no RLS; a provider's webhooks move `plan` / `status` / `current_period_end`), `0018` (`pack_review` + `pack_activation` — the two-person review and activation decision recorded against a pack's computed checksum; control plane, no RLS), `0019` (`pack_source_check` + append-only `pack_source_change` — the source-of-record watchlist and detected-change log; control plane, no RLS); `pnpm db:migrate`.
  - **Unit of work** (ADR 0004) — `pgUnitOfWork` / `inMemoryUnitOfWork`: business writes + the audit event + outbox messages commit in **one tenant-scoped transaction** or not at all.
  - **Outbox relay + events consumer** — `@rre/worker` drains unpublished `outbox` rows (`FOR UPDATE SKIP LOCKED`) and publishes them to SQS (`@aws-sdk/client-sqs`, LocalStack in dev); success sets `published_at`, failure bumps `attempts` and retries (at-least-once). A second loop, the **events consumer**, reads that queue, maps operator-facing events (`request.submitted` / `request.expired` / `entity.readiness_snapshot_created`) to `notification` rows (written as `rre_app` under the event's tenant), and best-effort delivers them to Slack (`SLACK_WEBHOOK_URL`) or the log. A message is `DeleteMessage`d only after its handler succeeds; a delivery failure doesn't lose the row.
  - **Operator web** — `@rre/web` (React + React Router): a control-pack list; **`ENT-001`** create screen whose scope-fact form is generated from `GET /packs/:packKey`; **`MAT-001`** matrix screen — entity-status banner (`BLOCKED` / `REVIEW_NEEDED` / `EVIDENCE_READY`), an honest denominator (never a %), applicability **and readiness** per control, the approved value, a clickable readiness-count filter, and an inline "add a claim" form (asserts → review, not approved); a **review queue** screen that approves / rejects (reason required) pending claims. Dev proxy `/api → :3000`.
  - **Slice 3 — contributor request loop** (`REQ-*` operator side + `SUP-*` no-account portal)
    - `@rre/api` operator routes: **`POST /entities/:id/requests`** (pick applicable controls, mint a request → returns the plaintext token **once** + `contributorPath`), **`GET /entities/:id/requests`**, **`GET /requests/:requestId`** (items, grant prefixes/uses, submission history — never the token), **`POST /requests/:requestId/send`** / **`/revoke`** / **`/resend`** (reissue — revoke live grants, mint a fresh link, reactivate a lapsed request), **`POST /submissions/:submissionId/items/:itemId/accept`** (pull one contributor response into the review queue as a `SUPPLIER_ASSERTION` `PENDING_REVIEW` claim).
    - **`/contributor/v1`** portal — the token is the principal, no tenant header; `referrer-policy: no-referrer`, `cache-control: no-store`, `x-robots-tag: noindex, nofollow` on every response. **`GET /requests/:token`** shows only the requested controls + the entity name (and any saved draft); **`PUT /requests/:token/draft`** saves in-progress answers (mutable, one per request); **`POST /requests/:token/submit`** takes an availability state per item, writes an immutable submission version, clears the draft, flips the request to `SUBMITTED`, and returns a receipt (`rcpt_…`). Invalid / expired / revoked tokens all return the same generic `INVALID_LINK`.
    - **Notifications** — `request.created` / `request.sent` / `request.revoked` / `request.submitted` / `request.link_reissued` / `request.expired` go through the same transactional outbox → relay → SQS path as readiness events, so downstream notifiers stay decoupled.
    - **Expiry sweep** — `@rre/worker` runs `sweepExpiredRequests()` on a timer (default 60s): requests with no unrevoked, unexpired grant move from a non-terminal status to `EXPIRED`, each with its own audit event + `request.expired` notification, all in one transaction. The cross-tenant transition is a `SECURITY DEFINER` function; the worker connects as `rre_app` holding only `EXECUTE` on it — `rre_relay` stays minimal.
    - Tokens are 192-bit, stored **hashed only** (sha256) with an 8-char lookup prefix; `access_token_grant` deliberately has no RLS and is resolved by hash **before** the tenant is known, then every downstream write runs under the resolved tenant's RLS.
    - **Web** — operator `RequestsPage` (`/w/entities/:id/requests`): pick applicable controls, set a message + link expiry, create a request, and see the contributor URL **once** with a copy button; `RequestDetailPage` shows status + grant prefix/uses, "draft in progress" if the contributor has started, requested controls, and each submission with an "accept into review" action per supplied value; can mark sent / revoke / **reissue** the link (new URL shown once). Contributor `ContributorPortalPage` (`/contribute/:token`) is served **outside** the operator shell — no workspace chrome; it shows only the requested controls, an availability state + value per item, a **Save progress** button that PUTs a draft (and rehydrates from it on return), no account needed, and a receipt on submit. Invalid / expired links get one generic message.
  - **Slice 4 — readiness snapshot + export**
    - `@rre/domain` `buildCanonicalExport()` — pure, deterministic (controls sorted by key, byte-stable `canonicalJson`), browser-safe. Produces the canonical export document (engine TRD §14.1): pack + source metadata, entity + classification facts, frozen evaluation reference, a verdict mapped from the entity status (**never** "compliant" / "certified"), readiness counts, per-control (clause, WCAG SC, applicability, readiness, approved claim), and an **exceptions** list = every required control not yet `EVIDENCED`. `canonicalExportToCsv()` renders the control matrix.
    - `@rre/api` — **`POST /entities/:id/readiness-snapshots`** freezes the current readiness into an append-only `readiness_snapshot` (canonical document + `sha256:` content hash), audits `readiness_snapshot.created`, enqueues `entity.readiness_snapshot_created`; **`GET`** list + detail; **`GET /readiness-snapshots/:id/export.json`** / **`export.csv`** return the document as an attachment, generated only from the frozen snapshot. A later claim approval never changes an existing snapshot — a new snapshot has a new hash.
    - **Web** — `SnapshotsPage` (`/w/entities/:id/snapshots`): "Create readiness snapshot", a list with status + content hash, and JSON / CSV downloads (fetched via `api.getText` and saved client-side, so no auth-header problem on a plain link).
  - **Slice 5 — notifications**
    - `@rre/worker` — `eventToNotification()` (pure map from a domain event to a notification, `null` for the operator's own actions and plumbing); `consumeEventsOnce()` (drain a batch from the events queue, handle-then-ack, leave a failing message for redelivery, drop an unparseable one); `pgNotificationWriter` writes rows as `rre_app` under the event's tenant; `slackWebhookNotifier` / `consoleNotifier` for external delivery.
    - `@rre/api` — **`GET /notifications`** (`?unread=1`, `?limit=`), **`GET /notifications/unread-count`**, **`POST /notifications/:id/read`**, **`POST /notifications/read-all`**. Read + mark-read only; the rows are written by the worker.
    - **Web** — `NotificationsPage` (`/w/notifications`) with per-item "Mark read" + "Mark all read" and a deep link to the request/snapshot; the `Shell` nav shows an unread badge, refreshed on navigation.
  - **Slice 6 — document intake + evidence store**
    - **`ObjectStore`** port — `createLocalObjectStore()` (bytes in memory, served by the API's own `/documents/content/*` route; the default for dev, tests, CI) and `createS3ObjectStore()` (presigned PUT into a quarantine bucket, `CopyObject` promote into originals, presigned GET — the ARCHITECTURE_AWS §4 shape). Selected by `S3_BUCKET_ORIGINALS` + `S3_BUCKET_QUARANTINE`.
    - **migration 0011** — `document` (RLS-scoped; the object key, hash, size, media type are immutable — only the lifecycle columns move, and rows are never hard-deleted) + append-only `document_association`.
    - **`@rre/api`** — **`POST /documents`** (validate media type + size, create an `UPLOADING` row, return a presigned/relative PUT URL), **`POST /documents/:id/finalize`** (fetch the bytes, SHA-256 them, run `scanBytes()` — media-type allow-list + size ceiling + EICAR — then promote quarantine→originals and set `AVAILABLE`, or `REJECTED_MALWARE` / `UNSUPPORTED`; audit + enqueue `document.available` / `document.rejected`), **`GET /documents`** (`?entityId=`), **`GET /documents/:id`**, **`GET /documents/:id/download`** (a URL only when `AVAILABLE`), **`POST /documents/:id/associations`**. A quarantined object is never downloadable.
    - **Web** — `DocumentsPage` (`/w/entities/:id/documents`): pick a file → initiate → PUT bytes → finalize; a list with status, size, and SHA-256; a client-side download for `AVAILABLE` documents.
  - **Slice 7 — evidence links + the `SELF_ATTESTED` state**
    - **`@rre/domain`** — a new readiness state `SELF_ATTESTED`: an approved claim with **no supporting document** is not `EVIDENCED`. `deriveControlReadiness` takes `evidencedByDocument`; `deriveEntityStatus` treats `SELF_ATTESTED` like `PENDING_REVIEW` (never `EVIDENCE_READY`); the export's exceptions list and `EXCEPTION_NOTE` gain the case.
    - **migration 0012** — append-only `evidence_location` (page / sheet / cell / bbox + quoted text + location hash) and `claim_evidence_link` (`SUPPORTS` / `CONTEXT` / `CONTRADICTS`), both RLS-scoped, `UPDATE`/`DELETE` revoked from `rre_app`.
    - **`@rre/api`** — **`POST /claims/:claimId/evidence`** (verify the claim + an `AVAILABLE` document, pin a location, link it, audit `claim.evidence_linked`), **`GET /claims/:claimId/evidence`**. The matrix + snapshot readiness now roll in whether each approved claim has a `SUPPORTS` link; `MatrixRow` gains `evidenceCount`.
    - **Web** — `ReviewQueuePage` gains an "Attach evidence" picker (an `AVAILABLE` document + optional page/quote) per claim; the matrix chip shows `SELF_ATTESTED` and a doc count.
  - **Slice 8 — AI / OCR extraction proposals**
    - **`Extractor` port** — the default `keywordExtractor` is deterministic and model-free (scans `label: value` lines, matches the label against a control's title/key words, splits a trailing unit). A Bedrock + Textract adapter plugs in behind the same port (a later `GATE`). Every proposal carries a source quote.
    - **`validateProposal`** — deterministic checks (empty → blocking `error`; malformed date / number / URL → non-blocking `warn`, per TRD §11.5).
    - **migration 0013** — `extraction_run` (adapter + model + schema version + input hash + status) and `extraction_proposal` (control, value, unit, method, confidence, page, quote, validation, status), both RLS-scoped and append-oriented — only the run's outcome and the proposal's decision columns move.
    - **`@rre/api`** — **`POST /entities/:id/documents/:documentId/extractions`** (run — document must be `AVAILABLE` and linked to the entity; audits `extraction.run`, enqueues `document.extracted`), **`GET`** list + **`GET /extractions/:runId`** (run + proposals), **`POST /extraction-proposals/:id/accept`** (the human gate — creates an `EXTRACTION_ACCEPTED` `PENDING_REVIEW` claim **and** the evidence link from the proposal's location; a proposal with no location or a blocking validator can't be accepted), **`POST /extraction-proposals/:id/reject`** (reason required).
    - **Web** — `ExtractionReviewPage` (`/w/entities/:id/documents/:documentId/extractions`): "Run extraction", proposals with confidence + source quote + validation findings, per-proposal accept / reject. Copy says "extracted proposals", never "verified facts".
  - **Slice 9 — applicability overrides**
    - **migration 0014** — `applicability_override` (result + rationale + source ref + effective evaluation id + optional expiry), RLS-scoped, append-only except `revoked_at`. It never touches the control snapshot or the frozen scope evaluation.
    - **`@rre/domain`** — `buildCanonicalExport` carries an `override` block per control and a top-level `overrides` list (`from` → `to` + rationale); an overridden-out required control stops being an exception.
    - **`@rre/api`** — `OverrideService` + **`POST /entities/:id/controls/:controlKey/applicability-override`** (validates entity, control, result; audits `applicability.overridden`), **`GET /entities/:id/applicability-overrides`**, **`POST /applicability-overrides/:id/revoke`**. `EntityService.matrix` and `SnapshotService` layer the active override (latest, unrevoked, unexpired) over the evaluated result before deriving readiness; `MatrixRow` gains `originalApplicability` + `overrideRationale`.
    - **Web** — `MatrixPage` gets an "Override" button + inline `OverrideForm` per control, and marks an overridden row "overridden from &lt;original&gt; — &lt;rationale&gt;".
  - **Slice 10 — readiness re-assessment**
    - **`@rre/domain`** — `diffEvaluations(before, after)` (pure, order-independent): `added` / `removed` / `applicabilityChanged` (`from` → `to`) / `unchanged`.
    - **`@rre/api`** — `EntityRepository.reEvaluate` (insert a new `entity_scope_evaluation`, move `regulated_entity.current_evaluation_id`; prior versions stay). **`POST /entities/:id/re-evaluate`** (optional corrected `facts`) re-runs `evaluateApplicability`, bumps the version, audits `entity.re_evaluated`, enqueues `entity.readiness_evaluated`, and returns the diff. Claims + evidence hang off the entity + control key, so they are untouched.
    - **Web** — `MatrixPage` gets a "Re-evaluate applicability" button and a change-summary panel (added / removed / applicability changes; "claims and evidence are unchanged").
  - **Slice 11 — control-snapshot impact**
    - **`@rre/domain`** — `diffControlSets(from, to)` → `added` / `removed` / `retained` control keys.
    - **`@rre/api`** — `EntityRepository.list()` (all of the tenant's entities + current evaluations); **`GET /packs/:packKey/impact`** — for the installed pack, which entities are on an older `snapshotKey`, and for each the added / removed control keys plus the count of approved claims now orphaned by a removed control. Adopting is `POST /entities/:id/re-evaluate`.
    - **Web** — `PackImpactPage` (`/w/packs/:packKey/impact`), linked from `PacksPage`: the impacted-entity table with added / removed control lists, an orphaned-claim count, and an "Adopt" (re-evaluate) button per row.
  - **Slice 12 — retention + deletion**
    - **migration 0015** — `deletion_request` (RLS-scoped; append-only except the completion columns; never deleted — it is the tombstone) + `purge_tenant(text)`, a `SECURITY DEFINER` function that `DELETE`s every tenant-owned row across all 21 tables (the append-only ones included) and returns the per-table counts.
    - **`@rre/api`** — `TenantAdminService` + **`GET /export/tenant`** (a JSON attachment: per-table counts + a full row dump for the tenant, audited `tenant.exported`), **`POST /deletion-requests`** (the `confirmation` must equal the workspace id; records a `REQUESTED` row with an impact preview, audits `deletion.requested`), **`GET /deletion-requests`**, **`POST /deletion-requests/:id/execute`** (re-checks the confirmation; in one transaction runs `purge_tenant`, marks the tombstone `COMPLETED` with the purged counts, then writes `deletion.completed` — the only surviving audit row for the tenant; afterwards best-effort deletes the tenant's objects). The `ObjectStore` gains `deleteTenant(tenantId)` (local + S3).
    - **Web** — `DeletionPage` (`/w/settings/deletion`, linked from the shell): "Download all data", then a double-confirmed delete (type the workspace id to request, type it again on the impact preview to execute), and a history list with the completion receipt.
  - **Slice 13 — accounts & tenancy** (the SaaS wrapper begins)
    - **migration 0016** — `app_user`, `tenant`, `membership` (`owner`/`admin`/`member`, `UNIQUE (tenant_id, user_id)`), `tenant_invite` (hashed token, 14-day expiry). No RLS: the control plane is read before a tenant context exists, so — like `access_token_grant` — every repo method is explicitly scoped by tenant or user id.
    - **`@rre/api`** — `rbac.ts` (`Role`, `can(role, capability)`), `AccountsService` + `AccountsRepository` (pg on a plain pool, in-memory for the default `buildApp` and tests). **`POST /sign-up`** (first sign-in mints the person + their first workspace as owner, audits `workspace.created`), **`GET`/`POST /workspaces`**, **`GET /members`**, **`POST /members/invites`** (owner/admin only, never mints an owner; token returned once), **`GET /members/invites`**, **`POST /members/invites/:id/revoke`**, **`POST /invites/accept`** (the invited person joins with their *own* verified email — mismatch/expired/revoked/used all refused), **`PATCH`/`DELETE /members/:userId`** (role change + remove, with a last-owner guard). The signed-in principal is resolved from `x-user-email` (dev stand-in for the eventual IdP session); existing routes still use the `x-tenant-id`/`x-actor` stand-in — wiring every route to require a `membership` is the next slice.
  - **Slice 14 — routes bound to membership**
    - **`@rre/api`** — `auth-hook.ts` — one `preHandler` on the `/api/v1` plugin resolves the signed-in principal (`principalFromRequest`) + their `membership` role in the `x-tenant-id` workspace, then decorates `req.auth` / `req.workspaceRole`. No principal → 401; not a member → 403. The control plane (`/sign-up`, `/workspaces`, `/members`, `/invites`) and public `/packs` are exempt (they self-enforce). `authFromRequest` now reads `req.auth`. Deleting a workspace (`POST /deletion-requests`, `.../execute`) is gated to `owner` via `can(role, 'delete_workspace')`. `buildApp({ devAuth })` — off in production; the test helper `buildTestApp` turns it on so a header-only caller is treated as an `owner` without a seeded membership.
  - **Slice 15 — operator web: sign-in, workspaces, team**
    - **`@rre/web`** — `session.ts` (`getUser`/`setUser`/`clearUser` — a localStorage stand-in for an IdP session; the client sends it as `x-user-email`), `SignIn` (email → identity), and a reworked `Shell`: no user → sign-in; user with no workspace → "create your workspace" (`POST /sign-up`); otherwise a real workspace `<select>` (from `GET /workspaces`) with "New", the signed-in email, and sign-out — the raw tenant text box is gone.
    - **`MembersPage`** (`/w/settings/members`, "Team" in the nav) — roster from `GET /members`; owner/admin can invite (`POST /members/invites`, link shown once), change a role (`PATCH /members/:userId`), remove a member, and revoke a pending invite; the last owner is protected in the UI too. **`JoinPage`** (`/join/:token`) — signs the person in if needed, then `POST /invites/accept`.
  - **Slice 16 — identity provider seam**
    - **`@rre/api`** — `auth/verifier.ts` — a `PrincipalVerifier` port so a real IdP drops in without touching routes or the membership hook. `headerVerifier()` is the dev stand-in (`x-user-email`); `jwtVerifier({ issuer, audience?, jwksUri | jwks })` accepts RS256 bearer tokens from any OIDC provider (Clerk, WorkOS AuthKit, Auth0, Cognito, self-hosted) — `auth/jwt.ts` verifies the PKCS#1 v1.5 signature against the JWKS with `node:crypto` (no dependency), checks `iss` / `aud` / `exp` / `nbf`, and maps `sub` → `userId`, `email`, `name`. The JWKS is fetched from `jwksUri` and cached. `index.ts` wires the JWT verifier when `AUTH_JWT_ISSUER` + `AUTH_JWKS_URI` are set, the header stand-in otherwise. The hook and the account routes now `await verifier.verify(req)`.
  - **Slice 17 — plans, subscriptions & limits**
    - **migration 0017** — `subscription` (one per workspace, control-plane, no RLS). Created on sign-up as a 14-day `trial`.
    - **`@rre/api`** — `billing/plans.ts` (`PLANS` — trial 3 entities / 3 seats, starter 25 / 10, growth unlimited), `BillingProvider` port with `noopBillingProvider()` (default — "checkout" returns to the billing page) and `stripeBillingProvider()` (Stripe's plain HTTP API, form-encoded, injectable `fetch`, no SDK), `billing/stripe-webhook.ts` (`verifyStripeSignature` — HMAC-SHA256 over `t.body` with a freshness window, `node:crypto`; `parseStripeEvent` → an internal `BillingEvent`). `BillingService`: `ensureTrial`, `summary` (plan + limits + live usage), `assertCanAdd(tenantId, 'entities' | 'seats')`, `checkout` / `portal`, `applyEvent`. Enforcement at the trust boundary — `EntityService.create` and `AccountsService.invite` refuse past the plan limit with a typed `QUOTA_EXCEEDED` / `SEAT_LIMIT` → **402**. **`GET /billing`**, **`POST /billing/checkout`** / **`/billing/portal`** (owner-only), and **`POST /webhooks/stripe`** (raw body, signature-verified, no session — outside `/api/v1`).
    - **Web** — `BillingPage` (`/w/settings/billing`, "Billing" in the nav): plan + trial/renewal date, a used-vs-limit table that flags a maxed resource, and Upgrade / Manage-billing buttons that redirect to the provider URL.
  - **Slice 18 — transactional email (invites)**
    - **`@rre/api`** — `email/sender.ts` — an `EmailSender` port with `consoleEmailSender()` (default — logs the message so dev/tests see what would go out) and `resendEmailSender()` (Resend's `POST /emails`, one JSON call, injectable `fetch`, no SDK). `email/templates.ts` — `inviteEmail(...)` → `{ subject, text, html }` (copy-guard-clean). `AccountsService.invite` now sends the invite email best-effort (the invite still stands and the one-time link is still returned if the provider is down). `index.ts` picks `resendEmailSender` when `RESEND_API_KEY` + `EMAIL_FROM` are set.
  - **Slice 19 — emailed contributor request links**
    - **`@rre/contracts`** — optional `recipientEmail` on `CreateRequestRequest` and `ResendRequestRequest`.
    - **`@rre/api`** — `email/templates.ts` gains `contributorRequestEmail(...)` (the no-account portal link, entity, optional note + due date; copy-guard-clean). `RequestService.createRequest` / `resend` email the portal link best-effort when a recipient is given — the one-time link is still returned to the operator, and a mail outage doesn't fail the request.
    - **Web** — `RequestsPage` gains an "Email the link to" field on the create form; `RequestDetailPage` an optional email beside "Reissue link".
  - **Slice 20 — guided onboarding**
    - **`@rre/api`** — `EntityService.list` + **`GET /api/v1/entities`** (every entity in the workspace with its current snapshot, newest first) — the web had no "all my entities" view or first-run signal before.
    - **Web** — `OnboardingPage` (`/w/onboarding`): a 3-step wizard — pick a regulation, add the first product/service (inline entity form reusing `FactField`), send the first evidence request (skippable) — ending on the one-time contributor link + a jump to the readiness matrix.
  - **Slice 21 — workspace dashboard**
    - **`@rre/api`** — `EntityService.list` now derives each entity's readiness roll-up (`entityStatus` + `readinessCounts`, honouring active overrides) so the list is a one-call dashboard feed.
    - **Web** — `DashboardPage` is now the landing view (`/`): a per-entity table with a status badge (`Evidence ready` / `Review needed` / `Blocked` / `Outdated snapshot`), a readiness one-liner, and quick links to the matrix / requests / documents / snapshots; the "Get started" card shows while the workspace has no entities. `PacksPage` moves to `/w/packs`.
  - **Slice 22 — a second control pack (regulation-agnosticism proof)**
    - **`packs/eu-battery-passport/`** — a `draft` bundle for the EU Batteries Regulation (2023/1542) battery-passport data: 8 controls across 6 families, 7 entity facts (battery category, EU-market, critical-metal content, due-diligence threshold, …), 7 applicability rules (family + control targets, `any`/`all`/`not`, an `OPTIONAL_IF_AVAILABLE` for LMT), 4 known-outcome vectors. **Zero engine code changed** — it is discovered, validated, and served through the same `/packs`, `/entities`, and matrix endpoints as the accessibility pack.
  - **Slice 23 — pack-driven presentation**
    - **`@rre/api`** — the matrix row now carries the control's `evidenceExpectation` (pack-authored "what good evidence looks like").
    - **Web** — the `entityKind` picker on the new-entity and onboarding forms is populated from the pack's own `entityKind` fact enum (and hidden when the pack allows only one kind — the battery pack shows no "service" option); `AddClaimForm` shows the control's `evidenceExpectation` as inline guidance. The battery pack narrows its `entityKind` enum to `["product"]`.
  - **Slice 24 — pack activation workflow**
    - **migration 0018** — `pack_review` + `pack_activation` (control plane, no RLS).
    - **`@rre/api`** — `PackGovernanceService` over the disk-loaded registry: `overview()` (per pack: on-disk status, computed checksum, validation issues, reviews for that checksum, activation, effective status, drift flag, blockers), `review` (records a reviewer against the *current* checksum), `activate` (needs the pack `valid` + ≥2 distinct reviewers on the current checksum → pins an activation row), `withdraw`. Platform-admin-gated **`GET /api/v1/admin/packs`**, **`POST /api/v1/admin/packs/:packKey/reviews`** / **`/activate`** / **`/withdraw`** (allowlist via `PLATFORM_ADMIN_EMAILS`). `GET /api/v1/packs` now reports the **governed** `status` (and keeps `onDiskStatus` alongside); if the bundle changes after activation the checksum no longer matches and the status falls back to `draft` with a `driftedSinceActivation` flag.
  - **Slice 25 — pack governance web view**
    - **Web** — `PackGovernancePage` (`/w/admin/packs`, "Pack governance" in the nav): the `overview()` as a table — effective status badge, on-disk status, short checksum, reviewer count + names, blockers, and per-pack "Add my review" / "Activate" (disabled unless `canActivate`) / "Withdraw" buttons. Drift and validation errors are flagged inline; a non-admin sees a plain "you are not a platform administrator" message.
  - **Slice 26 — build only against activated packs**
    - **`@rre/api`** — `EntityService.create` gains a `PackStatusSource` dependency + a `requirePackActivation` flag. When on, creating an entity against a pack whose **governed** status is not `active` is refused with `PACK_NOT_ACTIVE` → 409. `buildApp` default is off; `index.ts` turns it on unless `ALLOW_DRAFT_PACKS=1` (dev). Re-evaluation and read paths are unaffected — existing entities keep working if a pack is later withdrawn.
  - **Slice 27 — pack picker respects activation**
    - **`@rre/api`** — `GET /api/v1/packs` returns an `activationEnforced` flag alongside the pack list.
    - **Web** — a shared `pack-picker` helper: when `activationEnforced`, the new-entity and onboarding pickers only offer governed-`active` packs (with a "no activated regulations yet — an administrator needs to activate one" note when the list is empty); otherwise a non-active pack is shown with its status badged in the label (`EU Batteries Regulation — draft`).
  - **Slice 28 — pack source-of-record monitoring** (deterministic; the base a future AI agent plugs into)
    - **migration 0019** — `pack_source_check` (one row per watched URL: last hash, status, `etag`, error) + `pack_source_change` (append-only; one row per detected change, `acknowledged_*` the only mutable columns). Control plane, no RLS.
    - **`@rre/api`** — `PackSourceService` over the disk-loaded registry: `sweep()` walks every URL in every pack manifest's `sourceUrls`, fetches it (abortable timeout, conditional `if-none-match` on a stored `etag`, follows redirects), hashes the body, and on a hash change from a known prior raises an open `pack_source_change` carrying the affected pack keys; `304`/no-prior/unchanged just move the check row. `overview()` returns the per-URL last check + the open changes; `acknowledge(id, by)` closes one. Platform-admin-gated **`GET /api/v1/admin/pack-sources`**, **`POST /api/v1/admin/pack-sources/sweep`**, **`POST /api/v1/admin/pack-sources/changes/:id/acknowledge`** (409 `NOT_OPEN` if already closed). `PgPackSourceRepository` preserves the stored hash/`etag` when a patch omits them (`ON CONFLICT` with a per-column `CASE`). No worker loop yet — a cron or worker tick calling `sweep()` is a trivial follow-up.
    - **Web** — `PackGovernancePage` gains a "Sources of record" section: a "Run source check now" button, the list of open changes (URL + affected packs + an "Acknowledge" button), and a collapsible "last check per source" table.
  - **Testing** — 330 tests / 84 projects. Unit: readiness derivation (every precedence branch + entity-status roll-up), claim assert/approve/reject/supersede + reason enforcement, uow atomicity + audit pagination, applicability evaluator, pack validation, capability matrix, SQS command shape, request-loop token hashing + not-applicable-control rejection + expired-grant handling, draft round-trip + overwrite + clear-on-submit + outbox notification, resend (old link dies / new link works / reactivates a lapsed request / refuses a closed one); web screens (form from schema, matrix banner + readiness column + filter + inline claim + refetch, review-queue approve/reject with reason + server-error surfacing, request builder offering only applicable controls + one-time link reveal, request detail accept-into-review + revoke + reissue, contributor portal least-disclosure + no-account submit + generic invalid-link message + draft rehydrate + save-progress; sign-in gate + workspace switcher + first-workspace prompt, team roster + invite-link reveal + role change, invite acceptance; billing summary + trial + upgrade redirect, onboarding wizard walk-through, dashboard entity list with status badges + "Get started" card gating; pack-governance table with review/activate actions + drift flag + non-admin message; the pack picker offering only active packs when activation is enforced, badging draft packs when it is not). Integration (Postgres, `TEST_DATABASE_URL`, else skipped): RLS hides cross-tenant rows (entities, audit, claims, review decisions, requests, submissions, drafts) entirely; `WITH CHECK` rejects cross-tenant inserts; grants resolve by hash with no tenant context; `request_draft` is RLS-scoped **and** mutable (upsert overwrites, delete clears); the expiry sweep flips only lapsed non-terminal requests and writes their audit + outbox trail, cross-tenant, idempotently; a failed uow rolls back the entity **and** its audit event; `audit_event` / `review_decision` / `contributor_submission` `UPDATE`/`DELETE` denied to `rre_app`; `claim.value` is immutable to the app; the relay and the expiry sweep each run in their own disposable database; `readiness_snapshot` and `notification` are RLS-scoped, and `UPDATE`/`DELETE` are denied to `rre_app` (only `notification.read_at` is writable). Worker unit tests cover the event→notification mapping, `handleEvent` (row written even when external delivery throws), and the consumer's ack/redelivery/drop behaviour. `scanBytes` covers empty / oversize / disallowed-type / EICAR; `document` + `document_association` are RLS-scoped with only the lifecycle columns mutable and no association deletes; `evidence_location` + `claim_evidence_link` are RLS-scoped and append-only, and an approved claim with a `SUPPORTS` link reads as `EVIDENCED`; `extraction_run` + `extraction_proposal` are RLS-scoped with only the outcome/decision columns mutable; `applicability_override` is RLS-scoped and append-only except `revoked_at`; `deletion_request` is RLS-scoped and append-only except its completion columns, and `purge_tenant()` clears every tenant-owned row (the append-only tables included) across both tenants' data while leaving the other tenant and the tombstone untouched. The keyword extractor, `validateProposal`, `activeOverrides` (expiry + revocation precedence), `diffEvaluations`, and `diffControlSets` have unit coverage; `entity_scope_evaluation` re-evaluation adds a version and moves the pointer while the prior version stays, and `EntityRepository.list()` returns every tenant entity's current evaluation and nothing cross-tenant. `TenantAdminService` covers the export bundle, the confirmation-mismatch guard, and the execute path (purge + object cleanup + a single surviving `deletion.completed` audit row). `AccountsService` covers sign-up (person + workspace + owner membership + audit), person reuse across a second workspace, invite → accept (wrong-email / expired / revoked / already-used all refused), the invite-above-your-rank and member-cannot-invite guards, and last-owner demotion/removal protection; the pg suite adds the `membership` uniqueness constraint, the join-backed `listWorkspacesForUser` / `listMembers`, and that the control-plane tables are readable with no `app.tenant_id` set (no RLS). `verifyJwt` / `jwtVerifier` cover a good token, bad `iss` / `aud` / `exp`, a wrong-key or tampered signature, garbage, a missing-email claim, and JWKS fetch + caching; `headerVerifier` reads the `x-user-email` stand-in. `BillingService` covers trial creation (idempotent, 14 days), the usage summary, the entity + seat quota checks (and unlimited on growth), and applying checkout / update / cancellation events; `verifyStripeSignature` and `parseStripeEvent` cover a good signature, a wrong secret / tampered body / missing or stale signature, and the three event shapes; the pg suite round-trips the `subscription` repo (insert once via `ON CONFLICT DO NOTHING`, read, update, `getByCustomer`). `resendEmailSender` posts to the Resend API with the right auth + body and reports failure on a non-2xx; `consoleEmailSender` logs and reports ok; `inviteEmail` and `contributorRequestEmail` carry the one-time link and use no forbidden compliance language. `PackGovernanceService` covers the two-reviewer gate (same reviewer twice still counts once), activation flipping the effective status, withdrawal reverting it, and a stale-checksum activation being ignored + flagged as drift; the pg suite round-trips `pack_review` (dedupe on `(pack, checksum, reviewer)`) and the single-row-per-pack `pack_activation` upsert. **UAT** (`apps/api/src/acceptance/`): `AC-003`, `AC-004`, `AC-006`/`AC-007` (least-disclosure contributor link + no-account submission), `AC-008` (re-evaluate with a corrected fact → new version + diff → matrix follows → claims kept), `AC-009` (stale-snapshot entities listed → adopt clears them), `AC-010` (incl. `SELF_ATTESTED` → `EVIDENCED` after upload + link), `AC-012` (record override → derivation honours it → matrix + frozen export show it → revoke restores), `AC-014` (extraction run → PENDING proposals → accept → claim + evidence → `EVIDENCED`; reject needs a reason), `AC-016` (upload → scan → available → download; malware quarantined; unsupported type rejected), `AC-018`, `AC-021` (snapshot freeze + export-only-from-snapshot + immutability + no forbidden language), `AC-021b` (full data export → double-confirmed delete → every table emptied, the other tenant untouched, the `COMPLETED` tombstone kept, re-execution refused), `AC-030` (sign up → invite a teammate → they join with their own email → demote → member-cannot-invite → last-owner removal refused), `AC-031` (with the dev stand-in off: a member is let through, a signed-in non-member gets 403, the anonymous get 401, and requesting a workspace deletion is owner-only), `AC-032` (a `jwtVerifier`-wired app: an RS256 bearer token signs up and works on feature routes; no token → 401, an unverifiable token → 401, a valid token for a non-member → 403), `AC-033` (trial quotas cap entities and seats with 402, the summary tracks usage; checkout is owner-only; a signed `checkout.session.completed` webhook upgrades the plan and lifts the limit; a bad signature → 400), `AC-034` (an invite emails the invitee with the one-time `/join/<token>` link; a mail-provider outage does not break the invite), `AC-035` (a create request with a recipient emails the `/contribute/<token>` portal link; resend emails a fresh token; no recipient → no email). `GET /api/v1/entities` lists the workspace's entities newest-first with each one's `entityStatus` roll-up, and never another tenant's. `AC-036` creates and evaluates an entity against the **battery-passport** pack through the same endpoints — different facts, controls, and applicability, no engine change — and rejects facts that don't fit that pack's schema. `AC-037` records two reviews and activates a pack through the platform-admin routes (a non-admin gets 403, activation before two reviews is refused), the change shows through `GET /packs`, withdrawal reverts it, and — with `requirePackActivation` on — `POST /entities` against the still-`draft` pack is `PACK_NOT_ACTIVE` → 409 until it is activated. `PackSourceService` unit tests cover a first sweep recording every URL's hash, a re-sweep of the same bodies raising nothing, a changed body opening exactly one change with the right pack keys, acknowledge closing it (and refusing a second time), an `HTTP 500` recording `lastError`, and a `304` reading as unchanged; the pg suite round-trips `PgPackSourceRepository` (hash preserved across a patch that omits it, open-vs-all listing, acknowledge-once). `AC-038` sweeps the sources through the platform-admin routes (a non-admin gets 403), flips a watched body, sees one open change with its affected pack keys, and acknowledges it away.

Next: founder decision checkpoint — see [docs/BUILD_STATUS.md](docs/BUILD_STATUS.md). Identity is settled (host-native — AWS Cognito / GCP Identity Platform — behind the `jwtVerifier` seam, no third-party module); Stripe stays. Open: a substantive marketing-copy pass, wiring the real vendor adapters, a cron/worker tick for `PackSourceService.sweep()`, and which jurisdiction the next pack targets.

Next: contributor draft-save (`PUT /contributor/v1/requests/:token/draft`), and request notifications through the outbox.

## Build and develop

```bash
pnpm install
cp .env.example .env
pnpm infra:up          # local Postgres + LocalStack (zero AWS cost)
pnpm typecheck && pnpm lint && pnpm test && pnpm build
```

Full setup, workspace layout, and AWS free-tier / cost-control guidance: [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md).

## Proposed first pack

**EAA accessibility readiness.** Largest live market (EU Accessibility Act enforcement in force since June 2025), strong willingness to pay, soft mid-bottom competitive layer. The engine is built against this pack first; **CRA** (Cyber Resilience Act) is the proposed second pack, chosen to prove the pack abstraction on a very different domain.

## Platform

Build target is **AWS**, primary region **`eu-west-1`** (Ireland), managed services first, EU data residency. See [ADR 0001](docs/adr/0001-cloud-platform-aws.md) and the [AWS reference architecture](docs/ARCHITECTURE_AWS.md).
